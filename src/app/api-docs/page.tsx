import SwaggerUI from './SwaggerUI'

export const metadata = { title: 'API Docs — Venue Explorer' }

export default function ApiDocsPage() {
  return (
    <main>
      <SwaggerUI />
    </main>
  )
}
