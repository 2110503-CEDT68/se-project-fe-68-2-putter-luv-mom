FROM node:20-slim

WORKDIR /app

COPY package*.json ./
RUN npm ci
RUN npx playwright install --with-deps chromium

CMD ["sh", "-c", "npx playwright test; npx playwright show-report --host 0.0.0.0 --port 9323"]
